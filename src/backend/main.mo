actor {
  // The Tuition Payment Tracker app is fully frontend-driven.
  // All business logic and data storage is handled in the React frontend
  // using Zustand with localStorage persistence.
  // This backend serves as a placeholder required by the build system.

  public query func ping() : async Text {
    return "ok";
  };
}
